FROM mysql:5.6.40

ARG MYSQL_CONTAINER_USER
ARG MYSQL_CONTAINER_GROUP

RUN chgrp -R ${MYSQL_CONTAINER_GROUP} /var/lib/mysql

RUN usermod -u 1000 ${MYSQL_CONTAINER_USER} \
 && groupmod -g 1000 ${MYSQL_CONTAINER_GROUP}